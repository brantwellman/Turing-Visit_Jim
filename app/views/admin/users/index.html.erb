<h1>Current Clients:</h1>

<ul>
  <% @users.each do |user| %>
    <% unless user.admin? %>
      <li>
        <%= link_to user.name, admin_user_path(user) %>
      </li>
    <% end %>
  <% end %>
</ul>
